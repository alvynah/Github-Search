export class Repo {
    // tslint:disable-next-line:max-line-length
    constructor(public name: string, public description: string, public htmlUrl: string, public forks: number, public watchersCount: number, public language: string, public createdAt: Date) {
    }
}
