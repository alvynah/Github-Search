export class User {
    // tslint:disable-next-line:max-line-length
    constructor(public login: string, public repos: number, public followers: number, public following: number, public profile: string, public createdAt: Date, public htmlUrl: string) {

    }
}
